'use client';
import { useState,useTransition } from 'react';import { useRouter } from 'next/navigation';import { LockKeyhole,Plus,Trash2 } from 'lucide-react';import { createRecipeAction } from '@/app/painel/actions';
type Item={id:string;name:string;base_unit:string};type Product={id:string;name:string};type Recipe={id:string;name:string};type Component={type:'inventory_item'|'sub_recipe';id:string;name:string;quantity:number;unit:string;visible:boolean};
export function RecipeBuilder({items,products,recipes,initialProductId}:{items:Item[];products:Product[];recipes:Recipe[];initialProductId?:string}){const router=useRouter();const [pending,startTransition]=useTransition();const [productId,setProductId]=useState(initialProductId||'');const [name,setName]=useState('');const [yieldQty,setYieldQty]=useState(1);const [yieldUnit,setYieldUnit]=useState('un');const [confidentiality,setConfidentiality]=useState<'owner_only'|'authorized_team'|'protected_production'>('owner_only');const [notes,setNotes]=useState('');const [components,setComponents]=useState<Component[]>([]);const [error,setError]=useState('');function addItem(id:string){const item=items.find(i=>i.id===id);if(!item||components.some(c=>c.type==='inventory_item'&&c.id===id))return;setComponents(v=>[...v,{type:'inventory_item',id,name:item.name,quantity:1,unit:item.base_unit,visible:true}])}function addRecipe(id:string){const recipe=recipes.find(r=>r.id===id);if(!recipe)return;setComponents(v=>[...v,{type:'sub_recipe',id,name:recipe.name,quantity:1,unit:'g',visible:true}])}function save(){setError('');startTransition(async()=>{const result=await createRecipeAction({productId:productId||null,name,confidentiality,yieldQty,yieldUnit,preparationNotes:notes,components:components.map(c=>({type:c.type,id:c.id,quantity:c.quantity,unit:c.unit,visible:c.visible}))});if(!result.ok){setError(result.error||'Não foi possível salvar.');return}router.push('/painel/cofre');router.refresh()})}return <div className="grid gap-5 xl:grid-cols-[1fr_360px]"><section className="space-y-5"><div className="rounded-3xl bg-wine p-6 text-cream"><div className="flex items-center gap-3"><div className="flex h-12 w-12 items-center justify-center rounded-2xl bg-white/10"><LockKeyhole/></div><div><p className="m-0 text-xs font-bold uppercase tracking-[.18em] text-cream/50">Área confidencial</p><p className="m-0 mt-1 text-lg font-black">A fórmula é cadastrada diretamente no seu Cofre.</p></div></div><p className="mb-0 mt-4 text-xs leading-5 text-cream/60">Para controlar estoque, precisamos dos ingredientes, quantidades e rendimento. O modo de preparo é opcional.</p></div><div className="panel p-5"><p className="eyebrow mb-1">Identificação</p><div className="mt-4 grid gap-3 sm:grid-cols-2"><select value={productId} onChange={e=>setProductId(e.target.value)} className="input sm:col-span-2"><option value="">Sub-receita sem produto</option>{products.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><input value={name} onChange={e=>setName(e.target.value)} placeholder="Nome da fórmula" className="input sm:col-span-2"/><input type="number" min="0.0001" step="0.0001" value={yieldQty} onChange={e=>setYieldQty(Number(e.target.value))} className="input"/><select value={yieldUnit} onChange={e=>setYieldUnit(e.target.value)} className="input"><option value="un">unidades</option><option value="g">gramas</option><option value="kg">kg</option><option value="ml">ml</option><option value="L">litros</option></select><select value={confidentiality} onChange={e=>setConfidentiality(e.target.value as any)} className="input sm:col-span-2"><option value="owner_only">Somente proprietário</option><option value="authorized_team">Equipe autorizada</option><option value="protected_production">Produção protegida</option></select></div></div><div className="panel p-5"><p className="eyebrow mb-1">Composição</p><h2 className="mt-0 text-lg font-black text-wine">Ingredientes e sub-receitas</h2><div className="grid gap-2 sm:grid-cols-[1fr_auto]"><select id="ingredient-picker" className="input" defaultValue=""><option value="" disabled>Adicionar ingrediente do estoque</option>{items.map(i=><option key={i.id} value={i.id}>{i.name} ({i.base_unit})</option>)}</select><button onClick={()=>{const el=document.getElementById('ingredient-picker') as HTMLSelectElement|null;if(el?.value)addItem(el.value)}} className="h-11 rounded-2xl bg-wine px-4 text-xs font-bold text-cream">+ Ingrediente</button></div>{recipes.length>0&&<div className="mt-2 grid gap-2 sm:grid-cols-[1fr_auto]"><select id="subrecipe-picker" className="input" defaultValue=""><option value="" disabled>Usar sub-receita</option>{recipes.map(r=><option key={r.id} value={r.id}>{r.name}</option>)}</select><button onClick={()=>{const el=document.getElementById('subrecipe-picker') as HTMLSelectElement|null;if(el?.value)addRecipe(el.value)}} className="h-11 rounded-2xl border border-wine/10 bg-white px-4 text-xs font-bold text-wine">+ Sub-receita</button></div>}<div className="mt-4 space-y-2">{components.map((c,index)=><div key={`${c.type}-${c.id}`} className="grid items-center gap-2 rounded-2xl bg-cream/60 p-3 sm:grid-cols-[1fr_110px_90px_auto]"><div><p className="m-0 text-xs font-bold text-graphite">{c.name}</p><p className="m-0 mt-1 text-[10px] uppercase tracking-wider text-graphite/35">{c.type==='sub_recipe'?'sub-receita':'ingrediente'}</p></div><input type="number" min="0.0001" step="0.0001" value={c.quantity} onChange={e=>setComponents(v=>v.map((x,i)=>i===index?{...x,quantity:Number(e.target.value)}:x))} className="input !h-9"/><input value={c.unit} onChange={e=>setComponents(v=>v.map((x,i)=>i===index?{...x,unit:e.target.value}:x))} className="input !h-9"/><button onClick={()=>setComponents(v=>v.filter((_,i)=>i!==index))} className="mini-button text-danger"><Trash2 size={14}/></button></div>)}</div></div><div className="panel p-5"><p className="eyebrow mb-2">Opcional</p><textarea value={notes} onChange={e=>setNotes(e.target.value)} className="input min-h-32 py-3" placeholder="Modo de preparo ou observações internas. Você pode deixar em branco."/></div></section><aside className="panel h-fit p-5 xl:sticky xl:top-8"><p className="eyebrow mb-1">Cofre</p><h2 className="mt-0 text-xl font-black text-wine">Revisar fórmula</h2><div className="rounded-2xl bg-cream p-4 text-xs text-graphite/60"><p className="m-0"><strong>Rendimento:</strong> {yieldQty} {yieldUnit}</p><p className="mb-0 mt-2"><strong>Componentes:</strong> {components.length}</p><p className="mb-0 mt-2"><strong>Proteção:</strong> {confidentiality==='owner_only'?'Somente proprietário':confidentiality==='authorized_team'?'Equipe autorizada':'Produção protegida'}</p></div>{error&&<p className="rounded-2xl bg-danger/10 p-3 text-xs font-bold text-danger">{error}</p>}<button disabled={pending||!name||components.length===0} onClick={save} className="mt-4 flex h-12 w-full items-center justify-center gap-2 rounded-2xl bg-wine text-sm font-bold text-cream disabled:opacity-40"><LockKeyhole size={16}/>{pending?'Protegendo...':'Salvar no Cofre'}</button></aside></div>}
