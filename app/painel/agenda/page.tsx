import { PageHeader } from '@/components/page-header'; import { getBusinessContext } from '@/lib/auth'; import { createClient } from '@/lib/supabase/server'; import { shortDate, timeOnly, money } from '@/lib/format'; import Link from 'next/link';
export default async function AgendaPage(){const context=await getBusinessContext();if(!context)return null;const supabase=await createClient();const from=new Date();from.setHours(0,0,0,0);const until=new Date(from);until.setDate(until.getDate()+30);const {data:orders=[]}=await supabase.from('orders').select('id,order_number,status,scheduled_at,total,fulfillment_type,customer:customers(name)').eq('business_id',context.business.id).gte('scheduled_at',from.toISOString()).lt('scheduled_at',until.toISOString()).neq('status','canceled').order('scheduled_at');const groups=new Map<string,any[]>();for(const order of orders){const key=shortDate(order.scheduled_at);groups.set(key,[...(groups.get(key)||[]),order]);}return <><PageHeader eyebrow="Próximos 30 dias" title="Agenda" description="Visualize as encomendas pela data de retirada ou entrega." actionHref="/painel/pedidos/novo" actionLabel="Nova encomenda"/>{orders.length===0?<div className="panel p-10 text-center text-sm text-graphite/45">Nenhuma encomenda agendada para os próximos 30 dias.</div>:<div className="space-y-5">{Array.from(groups.entries()).map(([date,list])=><section key={date}><div className="mb-2 flex items-center gap-3"><span className="rounded-full bg-wine px-3 py-1 text-xs font-bold text-cream">{date}</span><div className="h-px flex-1 bg-wine/10"/></div><div className="panel divide-y divide-wine/5 overflow-hidden">{list.map((o:any)=><Link key={o.id} href={`/painel/pedidos/${o.id}`} className="flex items-center gap-4 px-5 py-4 hover:bg-wine/[.025]"><div className="w-14 text-sm font-black text-wine">{timeOnly(o.scheduled_at)}</div><div className="min-w-0 flex-1"><p className="m-0 text-sm font-bold text-graphite">Pedido #{o.order_number} · {Array.isArray(o.customer)?o.customer[0]?.name:o.customer?.name || 'Cliente não informado'}</p><p className="m-0 mt-1 text-xs text-graphite/40">{o.fulfillment_type==='delivery'?'Entrega':'Retirada'} · {o.status}</p></div><strong className="hidden text-sm text-wine sm:block">{money(o.total)}</strong></Link>)}</div></section>)}</div>}</>}
