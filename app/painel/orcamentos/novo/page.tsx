import {PageHeader} from '@/components/page-header';import {QuoteBuilder} from '@/components/quote-builder';import {getBusinessContext} from '@/lib/auth';import {createClient} from '@/lib/supabase/server';
export default async function NovoOrcamentoPage(){const context=await getBusinessContext();if(!context)return null;const supabase=await createClient();const[{data:customers=[]},{data:products=[]}]=await Promise.all([supabase.from('customers').select('id,name').eq('business_id',context.business.id).order('name'),supabase.from('products').select('id,name,base_price').eq('business_id',context.business.id).eq('active',true).order('name')]);return <><PageHeader eyebrow="Proposta" title="Novo orçamento" description="Monte uma proposta simples e depois transforme em encomenda com um toque."/><QuoteBuilder customers={customers??[]} products={products??[]}/></>}
